# 2026-08-28

## InnerOS 项目审计与修复

### 背景
用户给两份需求文档（InnerOS_V1.0 / V1.1）并要求按文档修改 `inneros` 仓库后由 GitHub Desktop 提交推送。
仓库当前已处于 V1.1 实现状态（P0–P4 均有 commit）。

### 审计结论
- 数据层：IndexedDB（entries / teams 两表），无登录/数据库，符合"账号后置"原则。
- 添加工作流：两步式（选类型→对应表单）已实现；电影=豆瓣搜索、书籍=Google Books，已删除评分/标签手填。
- 记录模型：entries[] 多次追加 + 照片上传 已实现。
- ❌ 违规点（已修复）：Sports（CS/足球）页面把第三方外链当主体——
  - 比赛卡片是 `<a href="hltv.org/...">`、焦点卡 `onclick=window.open`、顶部 scale-switcher 外链按钮、联赛卡外链。
  - 违反 V1.1 §11/§19「资源整合不是导航 / Sports 不以第三方跳转链接为主体」。

### 本次修改（app.js，未提交，待 GitHub Desktop）
- 移除 CS/足球 顶部外链导航按钮（scale-switcher）。
- 比赛卡片 / 焦点卡 改为结构化数据展示（div），仅保留底部极小「数据来源 · HLTV/懂球帝」链接（符合"极小入口"允许项）。
- 联赛卡由 `<a>` 改为信息展示 `<div>`。
- `node --check` 通过，无 `window.open` 残留。

### 仍存在的缺口（建议下一轮）
1. 音乐类型无 search→import 数据源（V1.1 明确要求"音乐必须搜索后导入"）—— 当前缺口最大。
2. Sports 仍是静态赛程数据，未真正 fetch/持续更新（Adapter 架构已预留，last_synced 缓存逻辑在但无真实请求）。
3. 事件详情未在顶部突出"地点+时间"（V1.1 §7.2）。
4. 资源页的"AI工具/常用资源"仍是外链列表——属资源导航概念，与 Sports 数据聚合不同，建议明确区分或弱化。

---

## V1.2 执行计划（InnerOS_V1.2_Agent_执行计划.docx）审计与修改

### 文档性质
V1.2 是 P0→P6 的 Agent 施工计划：先审计根因，再真实 Provider 替代 mock/硬编码，统一数据层，Sports 实时聚合，Memory 重构，收藏馆视觉。

### 审计结论（P0）
- Provider 层**已是真实的**：电影/音乐=iTunes Search、书籍=Google Books、游戏=FreeToGame，均免密钥、CORS 友好 → V1.2 §P1/§5 已达标。
- 详情页已明确分区「作品资料(客观) / 我的记录(个人)」→ §8 达标；Memory 追加模型 + last_synced_at 缓存已在 → §7/§P3 基本达标。
- ❌ 核心违规（V1.2 §6.1 / §12）：Sports 硬编码——
  - `CS2_TEAMS`/`FOOTBALL_TEAMS` 写死 12–18 队；`CS2_SCHEDULE`/`FOOTBALL_SCHEDULE` 用 `nextDate()` 生成假赛程；`toggleTeam` 存 `provider:'static'`。
  - 根因正是用户反馈的"搜不到球队"（只能从写死列表选）。
  - 阻塞：真实足球/电竞 API 需密钥，而 §12 禁止购买/提交密钥 → 真实 Provider 接入被阻塞。

### 本次修改（app.js，已改未提交，待 GitHub Desktop）
1. 事件/日记/地点详情顶部注入「📍地点 · 🕒时间」(heroMeta) → 满足 §7。
2. 球队选择器新增"手动关注任意球队/战队"输入框（addManualTeam，provider:'manual'）→ 直接缓解"搜不到"，标注为过渡方案。
3. 在 6 处加 `V1.2 §` 中文注释：Provider 层(§5)、Sports 静态注册表 GAP(§6.1/§12)、Seed 演示数据(§12)、toggleTeam 临时 provider(§6.1)、addManualTeam 过渡(§6.1)、事件详情顶部(§7)。
4. `node --check` 通过。

### 仍被阻塞 / 建议下一轮
- Sports 真实 Provider Adapter（需密钥，§12 禁止 → 待用户决定如何提供 key 或用免密钥源）。
- 音乐类型此前缺口：当前代码显示音乐已走 iTunes 真实搜索（与 V1.1 旧结论不一致，需复核）。
- 视觉重构(P5)、全量回归(P6) 未做。

---

## 用户提交流程偏好（本轮回应用户要求）

### 用户诉求
- 问：刚改的这些，GitHub Desktop 的 Summary / Description 该填什么？
- 要求：以后**直接**把 commit 文案写好给他（不必让他自己想）；并补写 README。

### 已做
- 给出可直接粘贴的 commit 文案（Summary + Description，均带 V1.2 § 条款引用）。
- README.md 顶部新增「2026-08-28 V1.2 Agent 执行计划合规修正」章节（feat/docs/阻塞，沿用仓库风格）。
- 长期记忆新增偏好：主动提供中文 commit 文案（遵循 `feat:` 约定）+ 代码注释 + README 同步。
- 提示：当前未提交改动含 app.js(V1.2) 与 .workbuddy/memory 工作日志；建议 Desktop 里只勾选 app.js 提交，memory 日志可单独或一并提交。

---

## 数据源更换实施（V1.2 审计后，用户要求"直接改、看效果"）

### 改动文件
- `functions/api/douban.js`（新增）：Cloudflare Pages Function，代理豆瓣 `subject_suggest`，电影/书籍归一化，免 Key。
- `app.js`：`searchMovie`/`searchBook` 改调 `/api/douban`（弃 iTunes-US / Google Books）；`searchMusic` 加 `country=CN`；`downloadImageAsDataURL` 对 doubanio 图源走 `/img?url=` 代理。
- `server.py`：新增 `/api/douban` 路由（本地预览与线上一致）。
- `README.md`：补"数据源更换"章节。

### 验证
- node --check app.js / py_compile server.py 均通过。
- 本地起 server.py，实测 `/api/douban`：星际穿越/Interstellar/百年孤独/三体 均正确返回中文结果+海报。
- 坑：端口 8765 有历史残留 python 进程会抢端口（致 404）；测试前需 `netstat -ano|grep 8765` + `taskkill /PID x /F` 清掉。

### 仍缺口（暂缓）
- 豆瓣详情接口 `/j/subject/` 被限流/改版 → director/简介/ISBN/出版社 为空；后续用详情页 HTML 解析或 TMDB 补全。
- Sports 真实 Provider 仍被 Key 阻塞（§12）。
