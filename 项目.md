# InnerOS 项目说明（项目.md）

> 面向人与 AI 的项目全貌。工程约定见 `AGENTS.md`，逐轮变更见 `CHANGELOG.md`，本文不重复细节。
> 更新时间：2026-09-20 · 当前版本 **v1.26.0**（记忆/资源整合成为显式父级 Hub，系统返回按产品层级而非访问历史；手机主界面任意非交互区域右滑可开侧栏；CS2 使用官方 Liquipedia API 的最多 200 场窗口并展示许可署名；其余 V1.25 记忆详情、相册、皮肤与图标能力保持不变）（版本号随每轮迭代递增；侧边栏页脚与设置页可见，用于确认部署是否成功）。
> 本文由 Agent 随功能/限制变化定期更新；逐轮变更见 CHANGELOG.md。

## 一句话

私人数字人生档案：电影/书籍/音乐/游戏/地点/事件/日记/速记统一记录在一条时间线上，多账户注册登录，所有设备实时同步，体育赛程聚合只看你关注的。

## 运行形态

```
浏览器（任意设备）                     Cloudflare（免费档）
  index.html + app.js                  Pages 静态托管
  IndexedDB = 离线缓存 + 操作日志  ⇄   Pages Functions（鉴权/同步/代理）
  唯一外部依赖：D1 数据库(DB 绑定)      D1 数据库 inneros-db
                                       Resend（验证码邮件，EMAIL_API_KEY）
```
- **线上主入口**：https://inneros.pages.dev（手机直接用，无需电脑）
- **本地预览**：`python server.py` → localhost:8765（`/api/auth`、`/api/sync` 反代线上，功能一致）
- 无框架、无打包、无付费服务、无密钥进代码（V1.2 §12 合规）

## 功能模块（2026-08-30 状态）

| 模块 | 说明 | 数据源 |
|---|---|---|
| 记录 | 电影/书籍/音乐/游戏/地点/事件/自定义，多次追加（entries[] 只增不覆盖）、多图；日记标题仅在详情原位编辑，媒体标题只读；首页摘要展示初记；详情按「初记/续写」篇章呈现，图片可全屏左右滑动 | 豆瓣 suggest+rexxar 详情（自动补全简介/导演/评分/出版社）、iTunes CN（音乐）、FreeToGame（游戏） |
| 速信 | 代替微信传输助手：文字/照片随手发给自己，按天气泡流，全设备同步（type=`quick`，不计入统计） | 自有 D1 |
| 时间线 | 朋友圈样式：每条直显 日期/时间/类型，点击进详情；日/月/季/年/人生五档 | 本地+云端 |
| 收藏 | 电影墙/书籍墙（想读/在读/已读）/音乐/游戏/地点，按年份组织 | 本地+云端 |
| 今天 | 今日记录（精确时间）+ ⭐收藏的赛程 + 那年今日 | — |
| 体育 | **V2 统一赛程中心**：足球+CS2 共用「我的主队 / 赛事 / 日期」三层结构；足球主队走单队接口，CS2 经 v1 Adapter/Service 拉官方最多 200 场窗口后按主队/赛事过滤，不再用赛事等级白名单丢数据；短 TTL + stale 降级，来源与许可在 UI 展示 | 足球=API-Football（环境变量 FOOTBALL_API_KEY，未配置回退 TheSportsDB）；CS2=Liquipedia MediaWiki API（CC BY-SA） |
| 账户 | 邮箱+密码注册登录（PBKDF2 10万次），Resend 邮箱验证码（配置 EMAIL_API_KEY 后强制），每人独立数据空间，Cookie 会话 90 天 | D1 + Resend |
| 工具 | 记忆/资源整合两个父级 Hub；按产品层级返回；手机主界面右滑开侧栏；全文搜索、那年今日、随机回忆、年度回顾、JSON 备份、4 套内嵌皮肤 | — |
| 桌宠 | AI 助手页的 2D 私人助手（序列帧动画，96 帧入库）：五种状态（idle/thinking/happy/error + 动作）、属性数值（心情/饥饿/体力/亲密）、左键随机互动效果（挥手/跳一跳）、右键开菜单（喂食/挥手/跳一跳/跳个舞/说句话/查看属性）、双击喂食、自由拖动（位置记忆）、待机微动作（眨眼/环顾/伸懒腰/整理衣服）、25~45 秒随机自动说话（饿了优先）、缩放/暂停/隐藏均可记忆；**纯前端，不接 AI 对话、不调第三方** | localStorage（键 `inneros_pet_state` / `inneros_pet_position`） |

## 同步协议（核心机制）

```
设备本地任意改动
→ 追加一条 operation 到本地 ops 队列（离线优先：未登录也入队，登录后补传）
→ syncNow(): push（50 条一批，op_id 幂等去重）
→ Worker 逐条事务落 D1 并分配单调 seq（冲突规则见下）
→ pull?cursor=设备last_seq（排除本机操作）→ 本地回放合并 → 推进游标
```
- **冲突规则**：追加条目永不冲突；元数据新者胜（败方存 `_conflicts` 不丢）；删除=墓碑优先（不复活）；同内容并发编辑双版本保留并标 `conflict`；照片先传附件后引用。
- **同步时机**：改动即时 / 60 秒定时 / 断网恢复 / 手动"立即同步"。
- **照片**：本地存原图 dataURL；上云前压缩 ≤1280px/JPEG0.8，存 attachments 表（一图一行）；>900KB 跳过云端。

## 部署与配置（Cloudflare）

| 项 | 值 | 谁配置 |
|---|---|---|
| Pages 项目 | inneros（git push 自动部署） | 已完成 |
| D1 绑定 | 变量名 `DB` → 数据库 inneros-db | 已完成 |
| 环境变量 | `EMAIL_API_KEY`（Resend API Key） | 已完成 |
| 环境变量 | `FOOTBALL_API_KEY`（api-sports.io 的 API-Football Key，V1.19 起足球主链需要；未配置自动回退 TheSportsDB） | 待用户决定 |
| Resend 域名 | 未验证（测试模式只能发给自己邮箱；验证 inneros.asia 需手动加 DNS） | 待用户决定 |

建表全自动（首次请求 `CREATE TABLE IF NOT EXISTS`），无需跑 SQL。

## 已知限制与遗留（不是 bug，勿重复排查）

1. 豆瓣详情走移动端 rexxar 接口（网页版反爬）；个别字段缺失属数据源限制。
2. 足球主链走 API-Football（需 CF 环境变量 `FOOTBALL_API_KEY`）：**免费档 100 次/天限流**，配额用尽或密钥异常时自动降级 TheSportsDB 旧数据源（首页有降级提示条，覆盖五大联赛+欧冠，主队/赛事查询降级期间不可用如实提示）；未配置 key 同样降级。**V1.20.2 起降级提示带上游 HTTP 状态码**（401=key 无效 / 403=封禁 / 429=限流），按提示去 Cloudflare Pages 变量页或 dashboard.api-football.com 排查。
3. Liquipedia 免费 MediaWiki API 当前由单次 ticker 模板展开**最多 200 场窗口**，不是完整历史数据库；不再按 A 级白名单过滤。赛事/战队名经中文映射表翻译（冷门赛事可能仍是英文）；详情页暂无地图比分/选手数据。赛事列表按基础名聚合（同赛事 A/B 组一张卡），页面展示 Liquipedia / CC BY-SA 署名。
4. 添加主队搜索：API-Football 只认英文名——中文常用队名（60+）自动转英文搜索，其余中文词建议用英文名或直接点热门推荐栏（12 支足球豪门 + 12 支 CS2 强队带真实队徽）。
5. **CS2 数据源为 Liquipedia 官方开放 API**（免费合规）。HLTV 无官方 API、Premium 订阅不含数据接口，网上 HLTV API 均为违反 ToS 的第三方爬虫，不可用也不必订阅；如需更专业实时数据须转付费数据商（GRID/Bayes/PandaScore，违背本项目无付费服务红线，不做）。
6. Resend 未验证域名前，验证码只能发 Resend 账号本人邮箱。
7. 忘记密码暂无法找回（无邮件服务）；注册验证码功能依赖上述 Resend。
8. 历史演示数据（置身事内/奥本海默等 17 条）已停止生成，并由 removeLegacySeeds 在各设备自动清理；云端残留由站长在 设置→管理员 清空/删除。
9. 云端照片为压缩版，原图仅存本地 IndexedDB。

## 数据与安全（重点）

| 数据 | 位置 | 保护 |
|---|---|---|
| 记录正文/元数据 | Cloudflare D1（免费档）+ 设备 IndexedDB 离线缓存 | 传输 TLS 加密；落盘由 Cloudflare 托管加密；按 user_id 隔离，账户间互不可见 |
| 照片（压缩版） | D1 attachments 表（同上） | 同上；原图仅在本机 |
| 密码 | 仅 PBKDF2-SHA256(10万次)+随机盐 的哈希，明文不落任何地方 | 拖库也无法反推明文 |
| 会话 | httpOnly Cookie（JS 不可读，防 XSS 窃取），90 天 | 退出即失效 |
| 账号切换 | 共用设备的本机缓存按账号隔离：切换账号自动清空上一账号的本机缓存并从云端拉取新账号数据 | 防串号 |
| 管理 | 站长 ADMIN_KEY（环境变量）+ 设置页管理员面板：账户列表/删除 | 密钥不进代码 |

尚未做（如需可立项）：端到端加密（服务端也看不到明文，代价是找回答份功能失效）、找回密码（需邮件服务已具备，可加）。

## 目录速览

```
index.html        单页外壳（全部内联 CSS）
app.js            前端生命周期、页面协调与兼容入口（功能按触碰范围渐进拆分）
src/features/     媒体、体育、记忆详情、页面层级与手势等独立前端模块
server.py         本地开发服务器（含线上 API 反代、豆瓣/体育代理和 CS2 v1 本地路由）
functions/        Cloudflare Pages Functions（v1 Route + Service + Adapter 分层）
AGENTS.md         AI 编码助手工作约定（数据契约/踩坑清单/省 token 约定）
CHANGELOG.md      逐轮变更记录（Keep a Changelog）
.audit-doc/       审计文档、接口抓包样本、本地测试工具
```

## 历史里程碑（详录见 CHANGELOG）

V1.1 智能记录与资源聚合 → V1.2 数据源真实化（豆瓣/rexxar/体育双源）→ 云同步 v1（WebDAV，已废）→ **多账户云同步 v2（D1+操作日志，现行）** → V1.4 速信/验证码/收藏制赛程/时间线朋友圈化。
